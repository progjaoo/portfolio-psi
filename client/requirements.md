## Packages
framer-motion | Page transitions and scroll-triggered animations

## Notes
No additional dependencies required. The contact form opens WhatsApp with the visitor's message filled in, which keeps the Vercel deploy static and avoids a Replit-only API dependency.
